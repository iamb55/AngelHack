Hello <%= @app.name %>,

You've been accepted as a mentor at Mentor.im.

Please click the link below to sign up:
<% if Rails.env == 'production' %>
  <% host = 'mentor.im' %>
<% else %>
  <% host = 'localhost:3000' %>
<% end %>
<%= 'http://' + host + '/mentors/sign_up?token=' + @app.token %>

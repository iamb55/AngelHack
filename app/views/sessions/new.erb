<!DOCTYPE html>

<head>
	<meta charset="utf-8" />

	<title>Mentor.im</title>
  <link href='http://fonts.googleapis.com/css?family=Lato:100,300,700,300italic' rel='stylesheet' type='text/css'>
  <link rel="shortcut icon" href="/favicon.ico" />
	<!-- Included CSS Files -->

	<link rel="stylesheet" href="app.css">
	<script src='jquery-1.7.2.min.js'></script>

	<!--[if lt IE 9]>
		<link rel="stylesheet" href="stylesheets/ie.css">
	<![endif]-->
	<script>
  	function getParameterByName(name)
    {
      name = name.replace(/[\[]/, "\\\[").replace(/[\]]/, "\\\]");
      var regexS = "[\\?&]" + name + "=([^&#]*)";
      var regex = new RegExp(regexS);
      var results = regex.exec(window.location.search);
      if(results == null)
        return "";
      else
        return decodeURIComponent(results[1].replace(/\+/g, " "));
    }
	  $(document).ready(function() {

	    $('#samewidth').on('click', function() {
	      $(this).val('');
	    });

	    $('#login').on('click', function() {
	      var data = {};
    	  if($('input[name="selectmentor"]:checked').val() == 'mentor') {
    	    window.location = '/start_session?type=Mentor';
    	  } else if ($('input[name="selectmentor"]:checked').val() == 'mentee') {
    	    window.location = '/start_session?type=Mentee';
    	  } else {
    	    return;
    	  }
	    });
	    
	    if(getParameterByName('type') == 'Mentor') {
	      $("input[value='mentor']").attr('checked', 'checked');
	    }
	    if(getParameterByName('type') == 'Mentee') {
	      $("input[value='mentee']").attr('checked', 'checked');
	    }
	  });
	  

	</script>

</head>
<body>


	<div id="wrap">
		<a href="../index.html"><img src="mentor-v.png" title="mentor.im" id="logo" /></a><br />

			<div id="landingcollect">

      
				<label for="radio1" id="purplelabel"><input type="radio" name="selectmentor" value='mentor'>&nbsp; Mentor</label> &nbsp; &nbsp;
				<label for="radio2" id="greenlabel"><input type="radio" name="selectmentor" value='mentee'>&nbsp; Mentee</label>
									<br /><br />


				<p><a href="#" class="large button radius" id="login">Login with Facebook</a></p>


			</div>
			</div>




</body>
</html>